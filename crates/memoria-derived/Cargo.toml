[package]
name = "memoria-derived"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
memoria-types = { version = "0.1.0", path = "../memoria-types" }
rusqlite = { version = "0.40.2", features = ["bundled"] }
thiserror = "2.0.20"

[dev-dependencies]
tempfile = "3.27.0"
